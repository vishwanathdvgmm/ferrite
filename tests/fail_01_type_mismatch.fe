// Verifies semantic analyzer strictly enforces type matching across branches

test fun main() {
    keep x: int = if true {
        5
    } else {
        "string" // ERROR: mismatched types
    };
}
