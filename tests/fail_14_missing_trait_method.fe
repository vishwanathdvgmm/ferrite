// FAIL: Impl block missing a required trait method
// Expected error: "Missing method"
trait Printable {
    fun display(self) -> string;
    fun debug(self) -> string;
}

group Widget {
    id: int;
}

// Only implements 'display', missing 'debug'
impl Printable for Widget {
    fun display(self) -> string {
        return "widget";
    }
}

fun main() {
    keep w: Widget = Widget { id: 1 };
}
