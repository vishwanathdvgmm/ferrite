// Test: pass_21_deep_recursion.fe
fun sum(n: int) -> int {
    if n == 0 {
        return 0;
    }
    return n + sum(n - 1);
}

fun main() {
    keep result: int = sum(10);
    assert(result == 55, "fail");
}
